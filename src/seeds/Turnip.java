/**
 * This contains the constructor for the seed Turnip inherting the Seed class
 */

package seeds;

import main.Seed;

public class Turnip extends Seed {
    /**
     * Constructs a turnip instance through the super keyword
     */
    public Turnip() {
        super("Turnip", "Root crop", 2, 2, 1, 5,6,5);
    }
}
